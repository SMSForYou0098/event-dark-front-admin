import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const CardReport = () => {
    return (
        <Card>
            <Title level={2}>Card Report</Title>
            <p>Card Report component - API integration pending</p>
        </Card>
    );
};

export default CardReport;
